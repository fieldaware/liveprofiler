machine:
  python:
    version: 2.7.9

test:
  override:
    - tox -- --junit-xml=$CIRCLE_ARTIFACTS/liveprofiler_tests_results.xml --cov liveprofiler --cov-report term-missing --cov-report html --cov-config=.coveragerccircle
